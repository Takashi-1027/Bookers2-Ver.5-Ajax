  <%= form_with(model:[book, book_comment], local: true) do |f| %>
    <%= f.text_area :comment,placeholder: "コメントをここに" , siza: "100%x6", class: 'form-control book_body' %>
    <%= f.submit "送信する" %>
  <% end %>